% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_qtbi.R
\name{estimate_qtbi}
\alias{estimate_qtbi}
\title{Estimate QTBI and append scores to a data frame}
\usage{
estimate_qtbi(
  data,
  chemicals,
  synergy_strength = 0.6,
  qtbi_col = "qtbi",
  percentile_prefix = "pct_",
  exposure_names = NULL,
  pct_cols = NULL,
  reference_doses = NULL,
  reference_index = NULL
)
}
\arguments{
\item{data}{A data frame containing exposure columns.}

\item{chemicals}{Character vector of exposure column names.}

\item{synergy_strength}{Synergy strength in \verb{[0, 1]}.}

\item{qtbi_col}{Name for the QTBI score column (default \code{"qtbi"}).}

\item{percentile_prefix}{Prefix for percentile columns (default \code{"pct_"}).}

\item{exposure_names}{Optional display names for plots (defaults to \code{chemicals}).}

\item{pct_cols}{Optional percentile column names (same length as \code{chemicals}).}

\item{reference_doses}{Optional named numeric vector of oral reference doses
in mg/kg/day, with names matching \code{exposure_names} (or \code{chemicals} if
\code{exposure_names} is omitted). The package derives potency weights as
\code{reference_index} dose divided by each component dose, rescales them so
their sum equals the number of exposures (keeping QTBI on the same
\eqn{[0, n]} scale as the unweighted index), and applies them at readout only.}

\item{reference_index}{Name of the index chemical for potency ratios. Defaults
to the first \code{exposure_names} entry (or first \code{chemicals} entry).}
}
\value{
A \code{qtbi_data} object (data frame) with QTBI and percentile columns.
}
\description{
Computes within-cohort exposure percentiles, runs the fixed entanglement
encoder, and returns the input data with percentile and QTBI columns added.
}
\examples{
\dontrun{
library(qtbi)
processed <- estimate_qtbi(
  my_data,
  chemicals = c("Pb", "As", "Cd", "Hg"),
  synergy_strength = 0.6,
  reference_doses = c(Pb = 6.3e-4, As = 6.0e-5, Cd = 5.0e-4, Hg = 1.0e-4),
  reference_index = "Pb"
)
}
}
